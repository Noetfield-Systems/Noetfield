# Alignment Scoring Engine (ASE)

```text
Score =
(Constitution Fit × 0.35)
+ (Runtime Feasibility × 0.25)
+ (Strategic Coherence × 0.20)
+ (Simplicity Gain × 0.10)
- (Drift Risk × 0.10)
```

Weights are canonical in `governance/strategy-alignment-map.json`.

Implementation target: OAS engine (future); policy veto today via Golden Edge v3 for forbidden actions.
